spring:
  datasource:
    hikari:
      jdbc-url: jdbc:h2:tcp://localhost/~/test
      driver-class-name: org.h2.Driver
      username: sa
      password:
    platform: h2

  profiles:
    active: local
  redis:
    host: 127.0.0.1
    port: 6380
    password:

  jpa:
    hibernate:
      ddl-auto: none

jwt :
  secretkey : yohan
